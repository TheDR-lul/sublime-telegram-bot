# Build artifacts (huge, rebuilt inside container anyway)
target/

# Git
.git/
.gitignore

# IDE and OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Docs and misc (not needed in image)
*.md
!README.md
.cursor/
terminals/

# Local config (secrets); image uses config.toml.example as default, runtime uses env vars
config.toml
